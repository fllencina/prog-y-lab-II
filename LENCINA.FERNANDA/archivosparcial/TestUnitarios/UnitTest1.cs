using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Entidades;
using Excepciones;
namespace TestUnitarios
{
   /* [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void ValidarErrorAlSerializar()
        {
            try
            {
                Votacion test = new Votacion("TEST",senadores);
                SerializarXML<Votacion> ser = new SerializarXML<Votacion>();
                ser.Guardar("c:\\", test);
            }
            catch (ErrorArchivoException e)
            {
                Assert.IsInstanceOfType(e, typeof(ErrorArchivoException));
            }

        }
    }*/
}
